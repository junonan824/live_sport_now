spring:
  application:
    name: live-sports-now
  
  # Redis 설정
  data:
    redis:
      host: localhost
      port: 6379
  
  # Elasticsearch 설정
  elasticsearch:
    uris: http://localhost:9200
  
  # Kafka 설정
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: live-sports-now-group
      auto-offset-reset: earliest

server:
  port: 8080 